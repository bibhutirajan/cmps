definition_version: '2'
entities:
  charge-mapping:
    type: streamlit
    identifier:
      name: charge_mapping
      database: SANDBOX
      schema: BMANOJKUMAR
    main_file: main.py
    query_warehouse: RD_ORG_WH
    stage: "charge_mapping"
    # Secrets commented out for sandbox deployment
    # secrets:
    #   snowflake_account: "applications.charge_mapping.snowflake_account"
    #   snowflake_user: "applications.charge_mapping.snowflake_user"
    #   snowflake_warehouse: "applications.charge_mapping.snowflake_warehouse"
    #   snowflake_database: "applications.charge_mapping.snowflake_database"
    #   snowflake_schema: "applications.charge_mapping.snowflake_schema"
    #   snowflake_role: "applications.charge_mapping.snowflake_role"
    artifacts:
      - environment.yml
      - main.py
      - config.py
      - db.py
      - deployment_config.py
      - components/**/*.py
      - static/css/styles.css
      - requirements.txt
      - .streamlit/config.toml

  charge-mapping_prd:
    type: streamlit
    identifier:
      name: charge-mapping
      database: applications
      schema: charge_mapping
    main_file: main.py
    query_warehouse: streamlit_charge_mapping_wh
    stage: "charge_mapping"
    secrets:
      snowflake_account: "applications.charge_mapping.snowflake_account"
      snowflake_user: "applications.charge_mapping.snowflake_user"
      snowflake_warehouse: "applications.charge_mapping.snowflake_warehouse"
      snowflake_database: "applications.charge_mapping.snowflake_database"
      snowflake_schema: "applications.charge_mapping.snowflake_schema"
      snowflake_role: "applications.charge_mapping.snowflake_role"
    artifacts:
      - environment.yml
      - main.py
      - config.py
      - db.py
      - deployment_config.py
      - components/**/*.py
      - static/**/*
      - requirements.txt
      - .streamlit/config.toml

